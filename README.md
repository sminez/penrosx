# Penrosx

Looking into what would be required for writing an OSX backend for [penrose][0].

I'm still uncertain as to whether the code in this repository should become part of the
main penrose crate or if it should be provided as a standalone crate that simply provides
a `Conn` implementation and helper functions for things like keybindings.

[0]: https://github.com/sminez/penrose
